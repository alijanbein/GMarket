import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "yellow",
    width: "100%",
    height: 60,
    marginTop: 15,
    alignItems: "center",
    flexDirection: "row",
  },
});
